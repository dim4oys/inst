# Collectors

Scripts in this folder fetch raw signals from external sources.

Current pilot:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/collectors/hacker_news.ps1 -Limit 30
```
